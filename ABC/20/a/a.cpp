#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define all(x) (x).begin(), (x).end()

using namespace std;
using ll = long long;

int main(void)
{
    int n;
    cin >> n;
    n--;
    if (n)
    {
        cout << "chokudai" << endl;
    }
    else
    {
        cout << "ABC" << endl;
    }
    return 0;
}